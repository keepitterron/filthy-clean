{
  "name": "filthy-clean",
  "version": "1.0.0",
  "description": "Filter and sanitize HTML input",
	"license": "MIT",
  "main": "src/filthy.js",
  "devDependencies": {
    "jest": "^22.0.6",
		"jsdom": "^11.5.1"
  },
	"scripts": {
		"test": "jest",
    "tag-release": "npm t && git commit -am $npm_package_version && git tag $npm_package_version && git push && git push --tags",
    "release": "npm run tag-release && npm publish"
	},
  "repository": {
    "type": "git",
    "url": "https://github.com/keepitterron/filthy-clean.git"
  },
  "author": "Claudio Semeraro",
  "keywords": [
    "filthy",
    "filter",
    "DOM",
    "sanitize",
    "sanitizer",
    "parser",
    "HTML",
    "xss"
  ]
}
